# Gradle
.gradle/
build/
**/build/

# Android
local.properties
*.apk
*.aab

# IntelliJ / Android Studio
.idea/
*.iml

# Local environment / secrets
.env
.env.local

# Android release signing. The beta keystore and its passwords are the owner's and
# stay outside the repository; the build reads them through -PchessKeystoreFile and
# friends. See docs/DEVELOPMENT.md and M17.1.
*.jks
*.keystore
keystore.properties

# OS
.DS_Store
Thumbs.db

# Temporary/editor backup files
*.bak
*.backup
*.orig
*~

# Supabase CLI scratch state (project ref cache, version check)
supabase/.temp/
